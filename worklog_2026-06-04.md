# 工作日志 — 2026-06-04

## DCC 识别逻辑全面优化

### 一、`*N` 数量后缀剥离

**问题**：`DCC-E*2` 被当作型号变体（"双拼单元"），实际 `*2` = 2 台。

**修正**：`*N` 后缀从型号中剥离，转为数量乘数。

| 影响文件 | 改动 |
|---|---|
| `full_analysis.py` | `extract_size_and_model()` 返回 5 值（+`qty_mult`）；`model_sort_key()` 删除 7 个 `*2` 条目；分组 `len()` → `sum(qty)` |
| `dwg_analyzer.py` | `enrich_insert_with_text()` 剥离 `*N` → `item["qty"]` |
| `combine_all.py` | `parse_spec()` + `model_sort()` 适配 |
| `make_schedule.py` | `parse_spec()` + `model_sort_key()` 适配 |
| `OBSERVATIONS.md` | `*2 = 双拼单元` → `*N = N台` |

---

### 二、标注尺寸推断数量

**问题**：两个 DCC 并排放，绘图员只写了一个型号 + 单台尺寸，没标数量。但块的实际尺寸是标准单台的整数倍。

**修正**：对比标注单台尺寸 vs INSERT 块实际几何，整数倍（≥2，3% 容差）→ 推断多台。

| 影响文件 | 新增 |
|---|---|
| `full_analysis.py` | `get_block_geometry()` 函数；收集循环中做宽/高比例推断 |
| `dwg_analyzer.py` | `get_block_geometry()` + `infer_dcc_qty_from_geometry()` |

---

### 三、跨图纸去重

**问题**：同一楼层拆分多张图纸（如 AR-01~04），同一设备在多张图里重复出现，被重复计数。

**修正**：
1. 从文件名自动提取楼层（`F3`、`B3`、`PB6` 等），无法识别时交互询问
2. 同楼层 + 同类别 + 同型号 + 同尺寸 + 坐标 ≤100mm → 合并为 1 个，来源图纸合并显示，数量取 max

数据验证：`cad/` 下 17 张 KEQP 主图，F3 楼层同专业拆分图（AR-01↔AR-04）有 149 处真实位置重复。

---

### 四、原点块定义过滤

**问题**：Revit 导出 DWG 在原点 `(0, 0)` 堆放块定义模板，不是真实设备。

**修正**：坐标 `abs(x)<10 and abs(y)<10` → 直接跳过，不统计。

---

### 五、建筑边界过滤

**问题**：图纸外侧的图例、安装说明区域（如 `DCC2850x2300` 系列块在 X=-76,000~-222,000）被当作真实设备。

**修正**：从轴网自动计算建筑外接矩形（外扩 5m 容差），超出边界的 INSERT 不统计。通用规则，不限特定块名。

---

### 六、型号名截断修正

**问题**：`DCC-3-1` → 被截成 `DCC-3`，`DCC-1-4` → 被截成 `DCC-1`。正则 `[-–]\d+.*$` 太贪心。

**修正**：
1. 捕获正则改为 `DCC-?\d+(?:-\d+)*`（支持多段编号）
2. 截断正则改为 `[-–]\d+[,，台].*$`（只砍数量后缀，不砍型号段）

---

### 七、`,N台` 数量解析

**问题**：标注 `DCC-4-1,8台` 被截成 `DCC-4`，丢了 `-1` 型号细分和数量 8。

**修正**：识别 `,N台` 后缀 → 型号 = 逗号前的完整名，数量 = N。

效果：
```
DCC-4-1,8台  → model=DCC-4-1, qty=8
DCC-3-1,10台 → model=DCC-3-1, qty=10
```

---

### 八、AI 图层自动发现（核心架构改进）

**问题**：设备分类依赖硬编码的块名正则（`DCC-Rack`、`DCC[-]?\d` 等），无法通用到新图纸。

**修正**：以图层为天然分类边界，AI 自动发现设备图层。

#### 工作原理

1. **扫描**：遍历所有图纸的所有 INSERT，按图层统计：
   - 总数量
   - 建筑边界内占比
   - 是否有实际几何
   - 块名是否可读（非 GUID）
   - 是否 XREF 引用

2. **评分**：对每个图层计算"设备评分"：
   - 大部分在边界内 → +2
   - 有实际几何 → +1
   - 可读块名 → +1
   - 排除标注层（ANNO/NOTE/GRID/标注等）
   - 排除系统层（Defpoints）
   - 排除模板层（STRUC-T 等）
   - 排除 0% 边界内的图例层

3. **分类**：根据图层名推断构件类别（DCC→DCC设备，DOOR→门，BEAM→结构 等）

4. **确认**：首次运行展示发现结果，用户确认后保存 `layer_config.json`，后续自动复用

#### 效果对比

| 指标 | 旧方式（块名正则） | 新方式（图层发现） |
|---|---|---|
| 分类依据 | 正则 `DCC-Rack|RACK|rack` | 图层映射表 |
| 新增图纸 | 改代码 | 自动发现 / 编辑配置文件 |
| 图层隔离 | 无，标注跨层串门 | 各自匹配 |
| 适用范围 | 只认 DCC | 门/窗/结构/管道/电气 全自动 |
| 发现图层数 | — | 22 个（当前项目） |

#### 识别结果（v13）

| 类别 | 数量 |
|---|---|
| DCC 设备 | 339 |
| DCC 支架 | 381 |
| 门 | 668 |
| 结构 | 227 |
| 设备（暖通/消防等） | 572 |
| 紧固件 | 78 |
| **总计** | **3,129** |

去重合并：101 个跨图纸重复（主要来自 F3 层同专业拆分图）。

---

### 修改文件清单

| 文件 | 改动类型 |
|---|---|
| `full_analysis.py` | 主要改动：8 项全部涉及 |
| `dwg_analyzer.py` | *N 剥离、原点过滤、尺寸推断 |
| `combine_all.py` | *N 剥离适配 |
| `make_schedule.py` | *N 剥离适配 |
| `OBSERVATIONS.md` | 文档修正 |
| `layer_config.json` | 新建：图层自动发现配置 |
| `result/all/设备材料表_v13.csv` | 最终输出 |

### 分支

`fixing_dcc_rec_issue` — 已推送到 origin。
